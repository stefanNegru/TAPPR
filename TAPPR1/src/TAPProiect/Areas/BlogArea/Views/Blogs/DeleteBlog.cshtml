@model TAP.Web.Areas.BlogArea.Models.BlogDeleteViewModel

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>BlogDeleteViewModel</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.BlogTitle)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.BlogTitle)
        </dd>

    </dl>
    @using (Html.BeginForm("DeleteBlog", "Blogs", FormMethod.Post))
    {
    @Html.HiddenFor(_ => _.BlogId)
    @Html.HiddenFor(_ => _.UserId)
    @Html.AntiForgeryToken()
    <input type="submit" value="DeleteBlog" class="btn btn-danger" />}
</div>
